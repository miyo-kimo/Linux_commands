## SSH Usage

SSH (Secure Shell) is a secure alternative to legacy protocols such as Telnet and Rlogin.

It enables encrypted communication between a control node and managed nodes and is widely used in automation tools like Ansible for remote system administration.
